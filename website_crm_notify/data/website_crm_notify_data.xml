<?xml version="1.0" encoding="UTF-8"?>
<odoo>
<data noupdate="1">

    <record id="website_crm_notify_mail" model="mail.template">
        <field name="name">Contact Form Notification</field>
        <field name="model_id" ref="crm.model_crm_lead"/>
        <field name="email_from">${object.company_id and object.company_id.email}</field>
        <field name="email_to">${(object.email_from)}</field>
        <field name="email_cc">${object.company_id and object.company_id.email},${object.team_id.user_id and object.team_id.user_id.email}</field>
        <field name="subject">[${object.company_id and object.company_id.name}] Thanks for your inquiry: ${object.name}</field>
        <field name="body_html"> <![CDATA[
<p>Thank you for contacting us.</p>
<p>We will get back to you shortly. If you need urgent help, please do not hesitate to contact us by phone.</p>
<p>Your inquiry details:</p>
<ul>
<li>Your Name: ${object.contact_name}</li>
<li>Phone Number: ${object.phone}</li>
<li>Email ${object.email_from}</li>
<li>Company: ${object.partner_name}</li>
<li>Subject: ${object.name}</li>
<li>Content: ${object.description}</li>
</ul>
<p>Best Regards,</p>
---<br>
${object.company_id.name or ''}<br>
${object.company_id.phone or ''}<br>
${object.company_id.email or ''}<br>
${object.company_id.website or ''}
        ]]></field>
    </record>

</data>
</odoo>
